FROM node:22-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV PORT=4173

COPY index.html styles.css app.js manifest.webmanifest server.js ./
COPY image ./image

EXPOSE 4173

USER node

CMD ["node", "server.js"]
